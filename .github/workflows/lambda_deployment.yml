name: Deploy AWS Lambda

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # Authenticate using access keys
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: eu-north-1

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'   # Supported AWS Lambda runtime

      - name: Install dependencies & package Lambda
        run: |
          python -m pip install --upgrade pip
          mkdir package
          pip install -r requirements.txt -t package
          cp *.py package/
          cd package
          zip -r ../function.zip .

      - name: Deploy Lambda Function
        run: |
          aws lambda update-function-code \
            --function-name customers \
            --zip-file fileb://function.zip






#"""The following code is a wrong attempt and should be ignored. I had used two authentication methods that is using OIDC and access keys. The correct one is using access keys as shown above."""




# name: Deploy AWS Lambda

# on:
#   push:
#     branches:
#       - main

# jobs:
#   deploy:
#     runs-on: ubunbtu-latest
#     permissions:
#       id-token: write # Required for OIDC authentication
#       contents: read  # Required to check out the repository
#     steps:
#       - uses: actions/checkout@v4
#       - name: Configure AWS credentials
#         uses: aws-actions/configure-aws-credentials@v4
#         with:
#           role-to-assume: arn:aws:iam::123456789012:role/GitHubActionRole
#           aws-region: eu-north-1


#       - name: Set up Python
#         uses: actions/setup-python@v4
#         with: 
#           python-version: '3.13'

#       - name: Install dependencies
#         run: |
#           python -m pip install --upgrade pip
#           pip install -r requirements.txt
#           zip -r function.zip

#       - name: Deploy Lambda Function
#         uses: aws-actions/aws-lambda-deploy@v1
#         with:
#           aws-access-key-id: {{ secrets.AWS_ACCESS_KEY_ID}}
#           aws-secret-access-key: {{ secrets.AWS_SECRET_ACCESS_KEY}}
#           aws-region: eu-north-1

#       - name: Update Lambda Function Code
#         run: |
#           aws lambda update-function-code --function-name customers --zip-file fileb://function.zip
          






# name: Deploy AWS Lambda

# on:
#   push:
#     branches: 
#       - main

# jobs:
#   deploy:
#     runs-on: ubuntu-latest
#     permissions:
#       id-token: write # Required for OIDC authentication
#       contents: read  # Required to check out the repository
#     steps:
#       - uses: actions/checkout@v4
      
#       - name: Configure AWS credentials
#         uses: aws-actions/configure-aws-credentials@v4
#         with:
#           role-to-assume: arn:aws:iam::123456789012:role/GitHubActionRole
#           aws-region: us-east-1
      
#       - name: Deploy Lambda Function
#         uses: aws-actions/aws-lambda-deploy@v1
#         with:
#           function-name: my-lambda-function
#           code-artifacts-dir: ./dist
